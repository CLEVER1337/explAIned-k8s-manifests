# cp grafana.env.example grafana.env and fill it in. grafana.env is gitignored.
#
# Only seeds the initial admin on an empty Grafana database. Once the PVC has been written
# once, the password lives there and editing this changes nothing.
admin-user=admin
admin-password=

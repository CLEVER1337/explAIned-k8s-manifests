# Dev only. Same value the repo's appsettings.json has always carried, so a developer moving
# between `dotnet run` and the cluster does not have to think about which password is where.
POSTGRES_PASSWORD=pass

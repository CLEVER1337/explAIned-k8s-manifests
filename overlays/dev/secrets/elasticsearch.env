# Dev only. Deliberately *not* the password from appsettings.json — that one leaked into a
# checked-in file and should be rotated out of existence rather than carried forward here.
ELASTIC_PASSWORD=changeme

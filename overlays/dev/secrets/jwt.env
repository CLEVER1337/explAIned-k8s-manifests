# Dev only. The symmetric key identity signs with and the other four services validate
# against — one value, one Secret, mounted into all five. HS256 wants at least 32 bytes;
# this is exactly 32.
JWT_KEY=JwtSecretPlaceHolder_explAIned32

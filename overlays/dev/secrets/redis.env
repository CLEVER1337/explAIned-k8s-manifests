# Dev only. Matches the password in the repo's connection strings.
#
# This value also goes into a redis:// URL for the Python services, so a replacement
# containing @ : / ? # or % has to be percent-encoded there. Sticking to [A-Za-z0-9_-] avoids
# the whole question.
REDIS_PASSWORD=gavno

# cp postgres.env.example postgres.env and fill it in. postgres.env is gitignored.
#
# This is the superuser password for the shared PostgreSQL instance — all four databases
# (asd, articles, comments, profiles) are reached with it. Rotating it means rolling the four
# services that compose it into a connection string, not just the StatefulSet.
POSTGRES_PASSWORD=
